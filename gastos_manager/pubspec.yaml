# pubspec.yaml gerado automaticamente
name: gastos_manager
description: Aplicativo de gestão de gastos
version: 1.0.0+1
environment:
  sdk: ">=2.17.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  # githubchat removido (não encontrado no pub.dev)
  # Dependências de IA sugeridas
  tflite_flutter: any
  google_ml_kit: any
  dialogflow_grpc: any
  flutter_tts: any
  speech_to_text: any
  # Outras dependências necessárias

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
